%--------------------------------------------------------------------------
%   绕Z方向的旋转矩阵
%--------------------------------------------------------------------------
function yaw = Z_rotation_degree(theta)
yaw = [cosd(theta) -sind(theta) 0;sind(theta) cosd(theta) 0;0 0 1];
end